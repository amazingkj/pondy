server:
  port: 8081

storage:
  path: ./data/pondy.db

retention:
  max_age: 30d
  cleanup_interval: 1h

targets:
  - name: mock-service
    type: actuator
    endpoint: http://localhost:9090/actuator/metrics
    interval: 10s
